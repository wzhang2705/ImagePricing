import React from "react";

const LikertScale = (props) => {

    return (
        <div className="bg-red p-2">
            <p className="text-left">{props.msg}</p>
        </div>
    );

}

export default LikertScale;